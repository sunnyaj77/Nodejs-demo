name: node installation & verifications

on: push

jobs:
  installation:
    runs-on: ubuntu-latest
    steps:
      - name: checkout code
        uses: actions/checkout@v3

      - name: installtion node.js and npm
        run: |
          sudo apt-get update
          sudo apt-get install -y nodejs npm
